# Виправлення проблеми з виділенням тексту кольорами

## Виявлені проблеми та виправлення

### Проблема 1: Неправильне обчислення позицій в оригінальному тексті ✅ ВИПРАВЛЕНО

**Проблема**: Функція `findPhraseInText` намагалася знайти позиції в оригінальному тексті, використовуючи позиції з нормалізованого тексту, що призводило до неправильних результатів.

**Виправлення**:
1. Спочатку виконується точний пошук (case-insensitive) в оригінальному тексті
2. Якщо не знайдено, виконується пошук з нормалізацією
3. При пошуку з нормалізацією слова шукаються окремо в оригінальному тексті з урахуванням нормалізації
4. Позиції обчислюються безпосередньо в оригінальному тексті

### Проблема 2: Занадто строга фільтрація за часом ✅ ВИПРАВЛЕНО

**Проблема**: Фрази пропускалися через невеликі розбіжності в часі між `textAnalysis` та сегментами.

**Виправлення**:
- Додано `timeTolerance = 0.5` секунди для більш гнучкої перевірки перекриття часу
- Тепер перевірка: `itemStart < segmentEnd + timeTolerance && itemEnd > segmentStart - timeTolerance`

### Проблема 3: Складність пошуку з нормалізацією ✅ ВИПРАВЛЕНО

**Проблема**: Пошук фраз з урахуванням нормалізації (видалення пунктуації) був складним та неточним.

**Виправлення**:
- Спрощено логіку пошуку: спочатку точний пошук, потім пошук по словах з нормалізацією
- Кожне слово шукається окремо в оригінальному тексті з урахуванням нормалізації
- Позиції обчислюються безпосередньо в оригінальному тексті

## Покращення логіки пошуку

### Нова логіка `findPhraseInText`:

1. **Точний пошук (case-insensitive)**: 
   - Шукає фразу в оригінальному тексті без нормалізації
   - Якщо знайдено, повертає точні позиції

2. **Пошук з нормалізацією**:
   - Нормалізує текст та фразу (видаляє пунктуацію, нижній регістр)
   - Шукає фразу в нормалізованому тексті
   - Якщо знайдено, знаходить слова в оригінальному тексті

3. **Пошук по словах**:
   - Якщо ціла фраза не знайдена, шукає послідовність слів
   - Кожне слово шукається окремо в оригінальному тексті
   - Позиції обчислюються на основі першого та останнього слова

## Тестування

Для перевірки виправлень:

1. Відкрити консоль браузера (F12)
2. Запустити workflow з overlap diarization
3. Перевірити логи:
   - `✅ highlightTextWithColors: Found highlights` - має з'являтися, якщо знайдені фрази
   - `🔴 highlightTextWithColors: No highlights found` - має з'являтися з деталями, якщо фрази не знайдені
4. Перевірити виділення в таблиці:
   - Синій колір для Blue (повторювані фрази)
   - Зелений колір для Green (overlaps)
   - Червоний колір для Red (розбіжності)

## Можливі проблеми, які залишилися

### Проблема з різницею в тексті між textAnalysis та сегментами

Якщо текст у `textAnalysis` відрізняється від тексту в сегментах (наприклад, через різні джерела транскрипції), фрази можуть не знаходитися.

**Рішення**: Перевірити, чи текст у `textAnalysis` відповідає тексту в сегментах. Можливо, потрібно використовувати той самий джерело транскрипції.

### Проблема з Unicode та спеціальними символами

Якщо текст містить Unicode символи або спеціальні символи, нормалізація може працювати некоректно.

**Рішення**: Можливо, потрібно покращити функцію `normalizeText` для підтримки Unicode.

## Додаткові рекомендації

1. **Додати більше діагностичного логування**:
   - Логувати знайдені фрази та їх позиції
   - Логувати порівняння нормалізованих текстів
   - Логувати результати пошуку для кожної фрази

2. **Покращити обробку помилок**:
   - Додати try-catch для обробки помилок під час пошуку
   - Логувати помилки для діагностики

3. **Оптимізувати продуктивність**:
   - Кешувати результати нормалізації
   - Використовувати більш ефективні алгоритми пошуку для великих текстів

